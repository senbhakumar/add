#include<iostream.h>
#include<string.h>
int main()
{
char s[50];
int i;
cout<<"enter the value";
cin>>s;
for(i=0;s[i]='\o';i++)

if(i==0)
{
s[i]=big(s[i]));
}
else

if(s[i]==' ')
{
s[i+1]=big(s[i+1]);
}
cout<<"display"<<s;
return 0;
}
